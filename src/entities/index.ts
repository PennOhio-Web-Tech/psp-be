import buildMakeOrder from "./order";
import buildMakeCategory from "./category";
import buildMakeProduct from "./product";

export const makeOrder = buildMakeOrder();
export const makeCategory = buildMakeCategory();
export const makeProduct = buildMakeProduct();
